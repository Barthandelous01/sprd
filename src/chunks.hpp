#ifndef CHUNKS_HPP_
#define CHUNKS_HPP_

#include <vector>
#include <string>

std::vector<std::string> chunks(std::string file);
void centerstring(const char *s);
void raw();
void unraw();

#endif /* CHUNKS_HPP_ */
